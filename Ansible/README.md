# Ansible

This directory contains Ansible configuration and playbooks for provisioning the MSPR1-Deploy-Odoo infrastructure.

## Getting Started

1. Install Ansible:
   ```sh
   pip install ansible
   ```

2. Run the example playbook:
   ```sh
   ansible-playbook -i inventories/hosts site.yml
   ```
